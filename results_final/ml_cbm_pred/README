>> perf_all.xslx
Performance of random forest classifier trained on all 5,933 features.

>> featimp_all.xlsx
Feature importances of 5,933 features in the random forest classifier trained on all 5,933 features.

>> perf_top50.xlsx
Performance of random forest classifier trained on top 50 features.

>> featimp_top50.xlsx.
Feature importances of 50 features in the random forest classifier trained on top 50 features.

>> perf_top50_noterm.xlsx
Performance of random forest classifier trained on top 50 features with features from C-terminal residues removed (i.e trained on 44 features).

>> poistion_rules.xlsx
Position-specific classification rules derived from top 50 features.

>> perf_top20.xlsx
Performance of random forest classifier trained on top 20 features.

>> featimp_top20.xlsx
Feature importances of 20 features in the random forest classifier trained on top 20 features.

>> perf_test_set.xlsx
Performance of random forest classifier trained on top 20 features on the test set.

